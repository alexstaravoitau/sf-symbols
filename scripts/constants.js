const SF_SYMBOLS_VERSION = "5.0";

const PATH_PRECISION = 2;

const FONT_SIZE = 18;
// Options are: "black", "bold", "heavy", "light", "medium", "regular", "semibold", "thin", "ultralight".
const WEIGHT = "regular";

module.exports = {
    SF_SYMBOLS_VERSION,
    WEIGHT,
    PATH_PRECISION,
    FONT_SIZE,
};
